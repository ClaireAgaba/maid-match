import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  build: {
    // Target older JS so that legacy Android WebViews (e.g. Chrome 72)
    // can execute the bundle without syntax errors like optional chaining.
    target: ['es2018', 'chrome72'],
  },
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
      }
    }
  }
})
